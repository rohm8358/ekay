[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ekay"
version = "1.0.0"
description = "EKay — concurrent, scope-locked MCP pentest orchestrator (HexStrike-class catalog, new architecture)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Rohm", email = "rohm8358@users.noreply.github.com" }]
dependencies = [
  "flask>=3.0.0",
  "waitress>=3.0.0",
  "mcp>=1.2.0",
  "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[project.scripts]
ekay = "ekay.cli:main"

[tool.setuptools.packages.find]
include = ["ekay*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
